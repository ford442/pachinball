# Arcade audio sample bank

Short OGG clips served from `/audio/*` and decoded on first user gesture.

Regenerate with:

```bash
bash scripts/generate-audio-samples.sh
```

Settings → **Sound Style** toggles between the sample bank and synth-only fallback.
