# Swarm State — #372 C++ revolute hinge + motor

## Objective
Add a 1-DOF motorized revolute (hinge) to the C++ physics core, expose it through
embind, have `wasm-owner` create one hinge per flipper, drive the motor from the
same input path as Rapier, stop stepping Rapier when no Rapier-owned table body
remains, and delete the kinematic capsule-proxy dual-step.

This is the last solver-capability gate before physics can move into a Web Worker
(#361) and before `wasm-owner` can become the default engine.

## Current Phase
**Done — Definition of Done met (2026-08-21)**

Core solver + embind + wasm-owner wiring landed on `main` via PR #376 (`d7073ed`).
This swarm run verified that work, fixed the remaining gates that were red, and
proved the Playwright flipper smoke in wasm-owner with `lastRapierStepMs === 0`.

## Files touched this iteration
- `.swarm-state.md` — replaced stale #359 state
- `src/game-elements/physics.ts` — TS narrowing after wasm-worker branch (`!== 'wasm-worker'`)
- `src/wasm/physics-worker.ts` — `postMessage` typed for DOM `self` (tsc)
- `src/wasm/PhysicsModule.ts` — resolve `./wasm/PhysicsModule.js` against the **page** origin
  (Vite was fetching `/src/wasm/wasm/PhysicsModule.js` → 404 → silent Rapier fallback)
- `src/game/physics/wasm-owner.ts` — comment that CAPSULE_AXIS is shape-axis alignment, not the
  retired kinematic proxy
- `docs/wasm-physics-engine.md` — Joints section note on capsule Y→blade X rest rotation
- `tests/countdown-portal-display.test.ts` — mock `setCharacterTakeActive` (unblocks 4 Vitest)
- `tests/wasm-owner-hinge.test.ts` — unit test that `ownerSkipRapierStep` skips Rapier
- `tests/wasm-owner-flipper.spec.ts` — smoke drives Digit1 + `stepPhysics` (no rAF hang)

## Last Green Checkpoint
| Check | Result |
|---|---|
| `npm run test:native` | **25/25** including 4 hinge cases |
| `npx tsc -b` | **exit 0** |
| `npm run lint` | **exit 0** |
| `npx vitest run` | **706 passed / 1 skipped / 0 failed** (86 files) |
| `npm run build:wasm` | **exit 0** (emcc 6.0.6, SIMD+LTO, installed `public/wasm/`) |
| `npm run test:wasm-parity` | **PASS** including `wasm hinge motor omega (wy=2.000)` |
| Playwright `tests/wasm-owner-flipper.spec.ts` | **1 passed** — raise launches ball, `rapierMs === 0` |

## Next Step
Pause here. Follow-ups (not this issue):
1. Flip `WASM_PHYSICS.defaultEngine` from `'rapier'` to `'wasm-owner'` once more
   table-level soak exists.
2. #361 worker+SAB can now own flippers (hinge APIs are on the worker command list).

## Open Decisions
- **defaultEngine still `'rapier'`** — intentional; flipping the default is a
  separate decision once smoke is proven (it now is).
- **Residual Rapier step** remains when **adventure mode** is active
  (`ADVENTURE_GROUP` bodies still live in Rapier). Table path sets
  `ownerSkipRapierStep` so `lastRapierStepMs === 0`.
- **`CAPSULE_AXIS_TO_BLADE_AXIS` kept** — native capsules are local +Y; Rapier
  flipper cuboids are local +X. The 90° Z rest rotation (and inverse remap onto
  the Rapier mesh puppet) is shape-axis alignment. `syncFlipperProxies` is gone;
  deleting the quat without a replacement stands the capsule vertically.
- **Kinematic capsules** remain for Catch2 flicking tests; they are not the
  wasm-owner flipper path.
- **Rapier mode** is unchanged (byte-identical gameplay path).
