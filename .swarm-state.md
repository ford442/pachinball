# Swarm State — #382 Ship wasm-owner as table physics default

## Objective

Make the in-house C++ WASM physics engine (`wasm-owner`) the production default
for table physics, with a fail-closed Rapier fallback when the bundle is missing.
Adventure mode continues to step Rapier. Expose `window.currentPhysicsEngine`,
source physics debug draw from packed C++ buffers, and add Playwright feel/scoring
coverage plus a native-CI default-flag assertion.

## Current Phase

Iteration final — Definition of Done met. Green checkpoint recorded below.

## Files touched this iteration

- `src/config/physics.ts` — `defaultEngine: 'wasm-owner'`; honor explicit `rapier`
- `src/game-elements/physics.ts` — `[Bootstrap][physics-degrade]`, `exposeCurrentPhysicsEngine`
- `src/game-elements/debug-hud.ts` — Physics panel `engine` label
- `src/game-elements/physics-debug-renderer.ts` — C++ packed buffers + Rapier overlay
- `src/game-elements/wasm-debug-geometry.ts` — codecs + AABB/sphere/capsule wires
- `src/game/physics/wasm-owner.ts` — debug colliders; bumpers kinematic (broadphase)
- `src/game/physics/wasm-static-export.ts` — return static debug descriptors
- `src/game/physics/physics-controller.ts` — push debug colliders to PhysicsSystem
- `tests/wasm-owner-flipper.spec.ts` — flipper + plunger + bumper score gates
- `tests/wasm-owner-adventure-rapier.test.ts` — skip Rapier iff adventure inactive
- `tests/physics-engine-preference.test.ts` / `tests/wasm-debug-geometry.test.ts`
- `.github/workflows/native-physics.yml` — `defaultEngine: 'wasm-owner'` grep
- `docs/wasm-physics-engine.md` — runtime default section
- `.swarm-state.md` — this file

## Last Green Checkpoint

| Command | Result |
|---------|--------|
| `npx tsc -b` | exit 0 |
| `npm run lint` | exit 0 |
| `npx vitest run` | 723 passed / 1 skipped / 0 failed (89 files) |
| `npx vite build` | exit 0 |
| `npm run test:native` | 25/25 |
| `npm run build:wasm` / `test:wasm-parity` | PASS (emcc 6.0.6 present) |
| Playwright `tests/wasm-owner-flipper.spec.ts` | 3 passed |

## Next Step

None for #382. Follow-ups: replay metadata still hardcodes `physicsEngine: 'rapier'`
in `src/game/game-lifecycle.ts` (out of allowlist); #383 C++ collision completeness
so static *spheres* can stay Static instead of kinematic bumpers.

## Open Decisions

- Replay metadata left unchanged (allowlist). Recommend recording `getWasmMode()`.
- No Settings physics dropdown existed; override is localStorage + HUD `engine`.
- Packed buffers peeked via duck-typed engine views (no `src/wasm/**` peek APIs).
- Bumpers in wasm-owner are **kinematic** spheres: C++ `BroadphaseGrid` skips
  Static rigid-body spheres (only `addStaticBox`/`addStaticCapsule` are paired).
  Changing native broadphase is #383.
