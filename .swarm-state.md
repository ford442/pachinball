# Swarm State — Refactor GamePhysicsController into src/game/physics/

## Last updated
2026-07-16T13:39:56.095Z

## Objective
Split `src/game/game-physics-controller.ts` (≈1708 lines) into focused modules under `src/game/physics/` without changing runtime behavior. Keep `GamePhysicsController` public API unchanged and preserve exact fixed-step order.

## Baseline
- `npx tsc -b` — clean.
- `npx vitest run` — 468 passed / 1 skipped / 0 failed.
- `npm run lint` — 0 errors, 3 warnings.

## Final module map
- `src/game/physics/types.ts` — `PhysicsHost` interface.
- `src/game/physics/mesh-interpolation.ts` — `MeshInterpolationSystem` (`syncMeshes`, `applyBindingPose`, `prevPoses`).
- `src/game/physics/wasm-mirror.ts` — `WasmMirror` class.
- `src/game/physics/scoring-bridge.ts` — `ScoringBridge` (combo/fever/tally/streak, `awardScore`, `handleBallLoss`, `startChainMultiball`, per-ball counters).
- `src/game/physics/scoring-multipliers.ts` — `getFeverScoreMultiplier` / `applyFeverGoldMultiplier` helpers.
- `src/game/physics/collision-dispatch.ts` — `CollisionDispatcher` (handle caches, collider→body conversion, body-pair debounce, obstacle dispatch, contact-force effects, portal sensor handling, collision counters).
- `src/game/physics/collision-handlers.ts` — per-obstacle reaction functions (`handleBumperCollision`, `handleFlipperCollision`, `handleTargetCollision`, `handleSpinnerCollision`, `handleBallTrapCollision`, `handleLauncherCollision`, `handleGateCollision`, `getBallMeshForBody`, `applySpinTransfer`).
- `src/game/physics/physics-controller.ts` — `GamePhysicsController` thin orchestrator (fixed-step order, input/nudge/tilt, WASM mirror lifecycle).
- `src/game/game-physics-controller.ts` — re-export shim.

## Completed
- Iteration 0: overwrote stale `.swarm-state.md`; removed unused `MIN_SPIN_FRAMES` in `tests/display-reels-telemetry.spec.ts`; verified baseline green.
- Iteration 1: created `src/game/physics/types.ts` with `PhysicsHost`; moved full implementation to `src/game/physics/physics-controller.ts`; converted `src/game/game-physics-controller.ts` to re-export shim; verified green.
- Iteration 2: extracted `MeshInterpolationSystem` into `src/game/physics/mesh-interpolation.ts`; left private `syncMeshes(alpha)` wrapper on `GamePhysicsController`; verified green.
- Iteration 3: extracted `WasmMirror` into `src/game/physics/wasm-mirror.ts`; verified green.
- Iteration 4: extracted `ScoringBridge` into `src/game/physics/scoring-bridge.ts`; wired `GamePhysicsController` to delegate scoring hooks; verified green.
- Iteration 5: extracted `CollisionDispatcher` into `src/game/physics/collision-dispatch.ts`; split out per-obstacle handlers into `src/game/physics/collision-handlers.ts` to keep modules under ~500 lines; preserved collider-pair + body-pair double debounce and all handle-space conversion; wired `GamePhysicsController` to delegate; verified green.
- Iteration 6: extracted fever/gold-ball multiplier helpers into `src/game/physics/scoring-multipliers.ts` so `scoring-bridge.ts` stays under 500 lines; updated re-export shim; final verification green.

## In progress
None — refactor complete.

## Blockers
None.

## Verification (Iteration 5)
- `npx tsc -b` — clean.
- `npx vitest run tests/collision-handle-space-scoring.test.ts tests/game-physics-controller-interpolation.test.ts tests/wasm-physics-parity.test.ts` — 9 passed / 1 skipped.
- `npx vitest run` — 468 passed / 1 skipped / 0 failed.
- `npm run lint` — 0 errors, 3 pre-existing warnings.

## Files changed
- `src/game/physics/types.ts` — created (`PhysicsHost`).
- `src/game/physics/mesh-interpolation.ts` — created (`MeshInterpolationSystem`).
- `src/game/physics/wasm-mirror.ts` — created (`WasmMirror`).
- `src/game/physics/scoring-bridge.ts` — created (`ScoringBridge`).
- `src/game/physics/scoring-multipliers.ts` — created (`getFeverScoreMultiplier` / `applyFeverGoldMultiplier`).
- `src/game/physics/collision-dispatch.ts` — created (`CollisionDispatcher`).
- `src/game/physics/collision-handlers.ts` — created (per-obstacle reaction functions).
- `src/game/physics/physics-controller.ts` — `GamePhysicsController` thin orchestrator.
- `src/game/game-physics-controller.ts` — re-export shim.

## Line counts (physics surface)
- `src/game/physics/physics-controller.ts` — 441 lines
- `src/game/physics/collision-dispatch.ts` — 380 lines
- `src/game/physics/collision-handlers.ts` — 337 lines
- `src/game/physics/scoring-bridge.ts` — 498 lines
- `src/game/physics/scoring-multipliers.ts` — 25 lines
- `src/game/physics/mesh-interpolation.ts` — 89 lines
- `src/game/physics/wasm-mirror.ts` — 114 lines
- `src/game/game-physics-controller.ts` — 13 lines (shim)

## Verification (final)
- `npx tsc -b` — clean.
- `npx vitest run tests/collision-handle-space-scoring.test.ts tests/game-physics-controller-interpolation.test.ts tests/wasm-physics-parity.test.ts` — 9 passed / 1 skipped.
- `npx vitest run` — 468 passed / 1 skipped / 0 failed.
- `npm run lint` — 0 errors, 3 pre-existing warnings.

## Summary for Jules wrap-up
`src/game/game-physics-controller.ts` has been refactored into a focused `src/game/physics/` package. The 1.7k-line god-object is now a 441-line orchestrator (`physics-controller.ts`) that only owns fixed-step order, input/nudge/tilt, and WASM mirror lifecycle. Collision handle-space conversion and dispatch live in `collision-dispatch.ts`, per-obstacle reactions in `collision-handlers.ts`, scoring/combo/fever state in `scoring-bridge.ts`, fever multiplier helpers in `scoring-multipliers.ts`, mesh interpolation in `mesh-interpolation.ts`, and the WASM mirror in `wasm-mirror.ts`. The public `GamePhysicsController` class name, constructor signature, and methods are unchanged; existing importers via `src/game/game-physics-controller.ts` continue to work. The behavior-lock tests (`collision-handle-space-scoring`, `game-physics-controller-interpolation`, `wasm-physics-parity`), the full Vitest suite (468/1/0), `tsc -b`, and lint (0 errors, 3 pre-existing warnings) all pass.
