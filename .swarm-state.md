# Swarm State — TrackManifest (#320)

## Objective
Refactor adventure-track registration behind a single `TrackManifest` surface.

## Current Phase
**COMPLETE** — all tracks migrated, all verify commands green.

## Tracks / Switch Arms
- **Migrated:** all 28 `AdventureTrackType` values via `track-manifest-data.ts`
- **Remaining:** none — `buildTrack()` switch removed; dispatches via `getTrackManifest()`

## Last Green Checkpoint
1. `npx tsc -b` — clean
2. `npm run lint` — 0 errors (3 pre-existing warnings)
3. `npx vitest run` — 572 passed / 1 skipped / 0 failed (+6 new manifest tests)
4. `npx vite build` — success
5. Sanity-grep — no new imports from `src/adventure/**` into `config.ts` or feeder files

## Blockers / Open Questions
- None

## What Changed
- New `src/adventure/manifests/` module: types, data, builders map, registry
- `TRACK_CATALOG` derived via `buildTrackCatalogFromManifests()`
- `ZONE_REGISTRY` derived via `buildZoneRegistryFromManifests()`
- `portal-routing.ts` anchors derived via `getManifestStartAnchor()`
- `AdventureMode.buildTrack()` → manifest dispatch (no switch arms)
- `TrackModeType` moved to `adventure-types.ts` (breaks circular dep)
- `docs/ARCHITECTURE.md` rewritten for manifest workflow
- `tests/track-manifest.test.ts` added (enum coverage + derived catalog/zone parity)

## Next Step
Ready for #321 (campaign-spine expansion / new track content). To add a track: enum + manifest entry + (JSON file or TS builder in builders.ts).
