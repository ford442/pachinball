# Swarm State — Slot Machine Mini-Game

## Last updated
2026-06-18T17:50:00+02:00

## Completed
- Added `SLOT_MACHINE_CONFIG` to `src/config.ts` (symbols, weights, paylines, payouts, activation modes)
- Added typed slot events to `src/game/event-bus.ts` (`slot:spin:start`, `slot:reel:stop`, `slot:win`, `slot:jackpot`, `slot:nearmiss`, `effect:slot:lighting`)
- Created pure-logic engine:
  - `src/display/slot-types.ts` (enums + interfaces)
  - `src/display/slot-logic.ts` (`generateSpin`, `checkWin`, `shouldActivate`, `recordActivation`)
- Created `src/display/slot-machine.ts` orchestrator:
  - REACH/FEVER activation with cooldown + chance/score gating
  - Staggered reel-stop scheduling reusing `DisplayReelsLayer` spring/settle physics
  - Jackpot → `jackpot:start` + points; wins → `points:awarded`; near-miss → `slot:nearmiss`
  - Debug: `localStorage` force-result + global `forceSlotSpin()` / `setSlotDebugForce()`
- Extended `src/display/display-reels.ts`:
  - Configurable symbol set + weights
  - `SpinOptions` for external speed/duration/stop-delay/target control
  - `stopReel(index)`, `forceResult()`, `setOnStopped()`
  - Preserved legacy auto-stop behavior when no options given
- Wired `src/display/display-core.ts`:
  - Creates `SlotMachine` on `subscribeToEvents()`
  - Calls `slotMachine.update(dt)` before `reelsLayer.update(dt)`
  - `configureSlotMachine()`, `startSlotSpin()`, `shouldActivateSlotMachine()`, `setSlotDebugForce()` delegates
- Wired `src/game-elements/sound-system.ts` with EventBus-driven slot SFX methods
- Wired `src/effects/effects-core.ts` to `effect:slot:lighting` for cabinet light sync
- Exported slot public API from `src/display/index.ts`
- Added `tests/slot-machine.test.ts` (28 Vitest tests covering spin RNG, win detection, activation modes)
- Updated `docs/PLAN.md` Slot Machine section with [IMPLEMENTED] tag and architecture notes
- Added `native/build` to `eslint.config.js` ignores (pre-existing generated-file lint error)
- Verified:
  - `npx tsc -b` ✅ clean
  - `npm run lint` ✅ 0 errors (3 pre-existing warnings)
  - `npm test` ✅ 406 passed (35 test files)
  - `npm run build` ✅ (including WASM build)

## In progress
Done — awaiting review.

## Blockers
none

## Files changed so far
- `.swarm-state.md`: project state tracking
- `src/config.ts`: added `SLOT_MACHINE_CONFIG`
- `src/game/event-bus.ts`: added slot + slot-lighting events
- `src/display/slot-types.ts`: new slot type definitions
- `src/display/slot-logic.ts`: new pure-logic engine
- `src/display/slot-machine.ts`: new slot mini-game orchestrator
- `src/display/display-reels.ts`: extended for config symbols / forced spins / per-reel stop
- `src/display/display-core.ts`: wired `SlotMachine`
- `src/display/index.ts`: exported slot public API
- `src/game-elements/sound-system.ts`: EventBus slot audio methods
- `src/effects/effects-core.ts`: slot lighting event handler
- `eslint.config.js`: ignore `native/build`
- `docs/PLAN.md`: marked slot section implemented
- `tests/slot-machine.test.ts`: new unit tests

## Next step
None — task complete. If reviewing, check that `display:set` REACH/FEVER path triggers spins in a browser run and that audio/lighting events fire as expected.
