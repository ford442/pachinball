# Swarm State — Native C++ Toolchain & Documentation Repair

## Objective
Stop C++ physics rot: untrack generated compile DB, harden native/WASM builds in CI, fix agent docs for the dual physics stack.

## Current Phase
**Iteration 0 — implementation in progress**

## Scope
- Untrack `compile_commands.json`; point `.clangd` at `native/build-native`
- Emscripten flag audit (O3, emmalloc, closure, SIMD+LTO defaults)
- Native `-Werror` + ASan/UBSan CI job
- Blocking WASM build + parity in `native-physics.yml`
- Update `CLAUDE.md` / `AGENTS.md`; reset stale swarm state
- Add missing barrel files (`src/core`, `engine`, `renderers`, `assets`, `style`)

## Last Green Checkpoint
- Pending full verification run

## Next Step
- Complete local verification (`tsc`, lint, vitest, native, wasm-parity)
