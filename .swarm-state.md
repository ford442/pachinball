# Fix First — red blocking smoke gate (menu occlusion + WebGL2 compile storm)

## Objective
Make "playwright smoke (desktop + mobile, blocking)" green by fixing two real bugs:
1. Menu occlusion: #cabinet-selector / map selector (desktop 1280x720) and #touch-controls (Pixel 7) sit on top of #start-btn in MENU.
2. WebGL2 shader compile storm: ~435-757 "BJS - Error compiling effect" on /?renderer=webgl2 (26 lights, too many shadow samplers).
DoD: see task brief (menu-reachability spec, 0 compile errors, 3 smoke specs green with CI=1, no force clicks, WebGPU unchanged, tsc/lint/vitest/build green).

## Current phase
Iteration 1 DONE (occlusion). Next: iteration 2 (WebGL2 compile storm).

## Files touched
- .swarm-state.md
- src/style/selectors.css — hide #map-selector/#cabinet-selector/#levels-selector while `#menu-overlay:not(.hidden)` (sibling selector; covers MENU + GAME_OVER; keyboard C/M/L still work; they reappear in PLAYING).
- src/style/mobile-responsive.css — hide #touch-controls while menu overlay shows; desktop-hide rule is now `(min-width:769px) and (pointer:fine)` (width-only rule hid controls on Pixel 7 landscape, 915px); landscape sizing also applies on `(max-height:500px) and (pointer:coarse)`.
- src/style/hud.css — #menu-overlay: `justify-content: safe center; overflow-y: auto` so tall menu content scrolls instead of clipping (Pixel 7 cabinet box is ~390px tall).
- src/style/controls.css — `.touch-btn { margin: 0 }` (global `button { margin-top:1rem }` leaked in), center stack gap 6 / margin-bottom 0, nudge 46px — plunger no longer towers >20px above flippers (mobile smoke portrait assertion, previously unreached).
- tests/menu-reachability.spec.ts (new) — elementFromPoint(centre of #start-btn) === #start-btn at 1280x720, 1366x768, 1920x1080, Pixel 7 portrait + landscape; selectors hidden in MENU and visible after a real Start click.
- tests/basic-gameplay.spec.ts — replaced `page.evaluate(() => startBtn.click())` bypass with `page.locator('#start-btn').click()`.

## Green checkpoint
Iteration 1 (commit "Stop in-play widgets occluding the menu Start button"):
- `npx tsc -b` → exit 0; `npm run lint` → exit 0; `npx vitest run` → 100 files passed | 1 skipped, 888 passed | 3 skipped.
- `npx playwright test tests/menu-reachability.spec.ts tests/verify_prism_core.spec.ts` (CI=1) → 7 passed.
- `npx playwright test tests/mobile-touch-smoke.spec.ts` → 1 passed.
- basic-gameplay still red: only on WebGL2 effect-compile console errors (iteration 2).

Visual check (1920x1080): headless WebGPU in this container stalls at StageDebug `scene_critical` on the BASE commit too (Start stays "Loading cabinet…"), so WebGPU screenshots are impossible here. Before/after taken on ?renderer=webgl2 instead (scratchpad, not committed): play frames at 1/2/3 s identical; menu differs only in that the cabinet/map/levels selectors are now hidden (intended). Light changes in iteration 2 must therefore be gated `!engine.isWebGPU` so the WebGPU path is unchanged by construction.
Measured on webgl2 1920x1080 base: 26 lights, 3032 console errors of which 435 "Error compiling effect".

Baseline (CI=1, 3 smoke specs): 3 failed.
- basic-gameplay: 757 console errors ("BJS - Error compiling effect", "Unable to compile effect").
- verify_prism_core: locator.click timeout — `<div id="cabinet-selector">` intercepts pointer events.
- mobile-touch-smoke: `.nudge-icon` / `.center-controls` from `#touch-controls` intercept pointer events.

## Local env note
Playwright 1.57 wants chromium(-headless-shell)-1200; container ships 1194. Workaround (no repo change):
scratch dir with symlinks `chromium_headless_shell-1200/chrome-headless-shell-linux64/chrome-headless-shell -> /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`
(+ same for chromium-1200/chrome-linux64/chrome), then `PLAYWRIGHT_BROWSERS_PATH=<that dir> CI=1 npx playwright test ...`.

## Blocked on
Nothing.

## Next 3 concrete steps
1. Measure on ?renderer=webgl2: scene.lights (name/type/includedOnlyMeshes), ShadowGenerators, engine.getCaps().maxTexturesImageUnits, which material fails.
2. Cap from the light/material side (maxSimultaneousLights and/or shadow gens only on key lights), gated to non-WebGPU.
3. Run basic-gameplay + all smoke specs with CI=1, count compile errors in MENU and 5 s after Start; commit.
