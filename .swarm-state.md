# Swarm State — First WASM Physics Slice (ball + bumper A/B)

## Last updated
2026-07-10T10:10:59+02:00

## Completed
- Plan approved: PhysicsSystem facade + GamePhysicsController mirror approach.
- WASM bundle built successfully.
  - Command: `bash scripts/build-wasm.sh`
  - Emscripten version: `emcc (Emscripten gcc/clang-like replacement + linker emulating GNU ld) 5.0.7 (263db4cffa6f9fc2ec514a70abac81362ea41849)`
  - Outputs: `public/wasm/PhysicsModule.js`, `public/wasm/PhysicsModule.wasm`
- Added `WASM_PHYSICS` config block + `getPhysicsEnginePreference()` to `src/config.ts`.
- Extended native C++ `PhysicsWorld` with `setBodyPosition` / `setBodyRotation`, exposed through `bindings.cpp`, rebuilt bundle successfully.
- Extended `src/wasm/PhysicsModule.ts` and `src/wasm/wasm-types.ts` with the new position/rotation proxies.
- Added engine-selection seam to `src/game-elements/physics.ts`:
  - Reads localStorage flag at init.
  - Loads `WasmPhysicsEngine`; falls back to Rapier with a warning if bundle missing.
  - `step()` routes to WASM when active; Rapier path unchanged.
  - `dispose()` frees WASM world.
- Added `WasmMirror` to `src/game/game-physics-controller.ts`:
  - Rebuilds WASM ball/bumper bodies on `rebuildHandleCaches()`.
  - Syncs Rapier→WASM before step and WASM→Rapier after step.
  - Routes `wasm:physics:contact` events through the shared dispatcher.
  - Refactored `processCollision` into `processCollisionBodies` so Rapier and WASM paths share scoring logic.
- Created `tests/wasm-physics-parity.test.ts` parity harness:
  - Unit layer (always runs) mocks a deterministic WASM engine and verifies mirror sync + scoring dispatch (4 passing).
  - Integration layer runs against the real WASM bundle and is gated behind `RUN_WASM_PARITY=1` (1 skipped by default).
- Fixed existing-test regressions caused by new `PhysicsSystem` seam methods:
  - Updated mocks in `tests/collision-handle-space-scoring.test.ts` and `tests/game-physics-controller-interpolation.test.ts` to include `isWasmActive` and `getWasmEngine`.
- Full verification loop clean:
  - `npx tsc -b` clean.
  - `npx vitest run` — 468 passed, 1 skipped, 0 failed.
  - `npm run build` — production build succeeds; WASM bundle rebuilt.
  - `npm run lint` — passes (3 pre-existing warnings only).

## In progress
None.

## Blockers
None.

## Files changed
- `.swarm-state.md`
- `src/config.ts`
- `native/src/PhysicsWorld.h`
- `native/src/PhysicsWorld.cpp`
- `native/src/bindings.cpp`
- `src/wasm/PhysicsModule.ts`
- `src/wasm/wasm-types.ts`
- `src/game-elements/physics.ts`
- `src/game/game-physics-controller.ts`
- `public/wasm/PhysicsModule.js`
- `public/wasm/PhysicsModule.wasm`
- `tests/wasm-physics-parity.test.ts`
- `tests/collision-handle-space-scoring.test.ts`
- `tests/game-physics-controller-interpolation.test.ts`

## Next step
Optional: run `RUN_WASM_PARITY=1 npx vitest run tests/wasm-physics-parity.test.ts` in a browser-like environment (jsdom or headless Chrome) to validate the real-engine integration layer, then consider promoting it from skipped to default-on once stable.
