# Swarm State — Restore Green Mainline (#319 gating slice)

## Last updated
2026-07-23T12:48:00Z

## Objective
Restore green mainline so all four gates pass on a fresh checkout:
- `npx tsc -b`
- `npm run lint` (0 errors)
- `npx vitest run` (0 failed)
- `npx vite build`

## Status
**COMPLETE — all acceptance criteria green.**

## Final verification
| Command | Result |
|---------|--------|
| `npx tsc -b` | clean |
| `npm run lint` | **0 errors**, 3 pre-existing warnings |
| `npx vitest run` | **555 passed** / 1 skipped / **0 failed** |
| `npx vite build` | succeeds (built in ~36s) |

## Baseline → final
- Vitest: 8 failed / 547 passed / 1 skipped → **0 failed / 555 passed / 1 skipped**
- Lint: 1 error (unused `physics`) → **0 errors**
- tsc / vite: stayed green

## What changed (by iteration)

### Iteration 0 — diagnose + overwrite state
Verified root causes against live failures. Noted free-map fail is `getLastTrackLoadError` (not `getLaneSensors`). Diagnosed lane debounce fail as **test-setup artifact** (not product bug): pair debounce + intentional `awardScoreCalls` reset on `ball:launched`.

### Iteration 1 — shared mock contract
- Added `tests/helpers/make-physics-host.ts` with `makeGameObjectsStub` (includes `getLaneSensors: () => []`), `makeBallManagerStub`, `makePhysicsHostShell`.
- Refactored `tests/wasm-physics-parity.test.ts`, `tests/collision-handle-space-scoring.test.ts`, and `tests/lane-sensor-scoring.test.ts` to use the helper (assertions unchanged).
- Fixed `tests/free-map-test-mode.test.ts` LevelLoader mock: stub `getLastTrackLoadError`.

### Iteration 2 — lane-sensor lint + debounce test
- Removed unused `physics` destructure (lint).
- Fixed "clears per-ball debounce on ball:launched":
  - Spy `performance.now` and advance +20ms past `COLLISION_DEBOUNCE_MS` (16) so pair debounce does not mask the under-test lane key clear.
  - Assert `awardScoreCalls` resets to 0 on `ball:launched` (ScoringBridge per-ball instrumentation), then returns to 1 after re-hit.
  - Keep cumulative `host.score === launch * 2` — proves debounce clear without weakening the behavioral claim.
- **No product change** to `collision-dispatch.ts` — `laneRolloverAwardedKeys.clear()` on `ball:launched` is correct.

### Iteration 3 — full gate + STOP
Full suite green; vite build links. Local gitignored `native/build-*` dirs were removed only to make `npm run lint` match fresh-checkout (eslint ignores `native/build` but not sibling build dirs; CI/fresh clone never has them).

## Files changed
- `tests/helpers/make-physics-host.ts` — **NEW** shared PhysicsHost mock helper
- `tests/wasm-physics-parity.test.ts` — use helper (`getLaneSensors` stub)
- `tests/collision-handle-space-scoring.test.ts` — use helper (`getLaneSensors` stub)
- `tests/free-map-test-mode.test.ts` — stub `getLastTrackLoadError`
- `tests/lane-sensor-scoring.test.ts` — use helper; lint fix; debounce test timing + correct post-reset counter assertion
- `.swarm-state.md` — this run's state

## Files NOT changed (per boundaries)
- `src/game/physics/collision-dispatch.ts` — product debounce-reset is correct
- `src/config.ts`, feeders, display, shaders, native, wasm, adventure, CI workflows

## Blockers
None.

---

## Summary for Jules wrap-up

Restored green mainline for the #319 gating slice. The #292 lane-rollover feature called `gameObjects.getLaneSensors()` from `CollisionDispatcher.rebuildHandleCaches()` but three PhysicsHost test mocks omitted that method, so rebuild threw and cascaded into seven failures across wasm-parity / handle-space / (plus a related free-map mock gap for `getLastTrackLoadError`). A shared helper (`tests/helpers/make-physics-host.ts`) now stubs `getLaneSensors: () => []` once and the three host builders consume it. The eighth failure — lane-sensor "clears per-ball debounce on ball:launched" — was a test-setup artifact: emitting `ball:launched` correctly clears `laneRolloverAwardedKeys`, but the immediate re-step was blocked by the 16ms collider/body pair debounce and `awardScoreCalls` is intentionally reset to 0 by ScoringBridge; the test now advances `performance.now`, asserts the counter reset, then proves a fresh award via `awardScoreCalls === 1` and cumulative `score === launch * 2`. No product code changes. Final gates: tsc clean, lint 0 errors / 3 warnings, vitest 555 passed / 1 skipped, vite build OK. **STOP.**
