# Pachinball — 2026-07-23 (Thursday planner dispatch)

**Status:** 🔴 `origin/main` is red — `tsc -b` clean, but Vitest 8 failed / 547 passed / 1 skipped and lint 1 error. Today is Fix First: restore green mainline (the gating P0 of new issue #319).

## Mode declaration
**FIX FIRST** — a fresh-clone health check found mainline unit CI red (8 Vitest fails + 1 lint error), single-root-caused to the #292 lane-sensor scoring landing breaking the shared test host-mock contract; a cracked foundation is the routine's mandatory priority and this is exactly the gating slice of Noah's new P0 #319.

## Context from prior sessions
- **Same-day-last-week (2026-07-16) landed as planned:** the #282 `GamePhysicsController` split shipped — the controller is now a 13-line re-export shim over an 8-module `src/game/physics/` package. `#283` CI also landed (`.github/workflows/ci.yml` gates tsc/lint/vitest/vite-build). Both moved to Done.
- **New in-context roadmap (2026-07-23):** Noah filed an ordered strategic sequence **#319→#320→#321** (+ #322/#323 parallel), explicitly gated on #319 first. This supersedes the older #282–#304 dump and is now the primary Ideas source.
- **The crack:** the #292 lane-rollover sensor scoring feature landed *direct-to-main* (`codespace`/Cursor commits `4c6a24c`/`afaafac`/`506cc78`), adding `getLaneSensors()` to the real `GameObjects` (`src/objects/object-core.ts:387`) + a call in `CollisionDispatcher.rebuildHandleCaches()` (`collision-dispatch.ts:159`), but never updated the shared test host-mocks. Runtime is fine; three test files throw and a fourth has a real behavioral fail + lint error.
- **This is #319's thesis proven:** a red-on-main commit got through because e2e is soft-gated and the direct push likely raced/ignored the blocking unit job. CI hardening is genuinely urgent.
- **Context gaps flagged:** ① `recent_chats` / `conversation_search` connectors are **unavailable in this remote GitHub environment**, so there's no cross-project chat history for this run — repo + issues + `weekly_plan.md` are the whole evidence base. ② I did not run the debounce-reset failure to ground truth (real bug vs. test-setup artifact) — kimi-cli must determine that. ③ `#288` (deploy creds) is marked open on GitHub but PR #314 ("Remove hardcoded deploy credentials") appears merged — verify before treating as open.

## weekly_plan.md changes written
- **Today's focus** → rewritten to Fix First / restore green mainline (full root-cause + boundaries).
- **Ideas** → #282 and #283 marked `[done — 2026-07-23]`; new #319–#323 sequence added as the ordered primary Ideas source; #319 marked `[in progress — 2026-07-23]`.
- **Backlog** → added the 🔴 red-mainline root-cause item and the #319 CI-hardening item; resolved the stale "No CI still (#283)" item; updated the `.swarm-state.md` reset note for the new run.
- **Done** → added #282 split, #283 CI, the #319–#323 reconciliation, and the red-mainline discovery.
- **Last run** → set to 2026-07-23 Fix First; prior 2026-07-16 run folded into history with its landed outcome.

## Today's focus
**Restore green mainline** — make `npx tsc -b`, `npm run lint`, `npx vitest run`, and `npx vite build` all green on `main`, which is the first acceptance criterion of **P0 #319**. Concretely: repair the shared test host-mock contract (`getLaneSensors`) across `wasm-physics-parity`, `collision-handle-space-scoring`, and `free-map-test-mode`; diagnose and fix the `lane-sensor-scoring.test.ts` "clears per-ball debounce on `ball:launched`" failure (real debounce-reset bug vs. test setup); clear the unused-`physics` lint error. No new feature work until green.

---

# Dispatch

## A. kimi-cli Swarm Task — restore green mainline (the main event)

```
You are running an autonomous, iterative swarm loop on the Pachinball repo (TypeScript / Vite / Babylon.js 7 / Rapier3D WASM). Your single objective for this run:

  RESTORE A GREEN MAINLINE. Make all four of these pass on a fresh checkout:
    npx tsc -b          # (already green — keep it green)
    npm run lint        # currently 1 error
    npx vitest run      # currently 8 failed / 547 passed / 1 skipped
    npx vite build      # (verify it still links)

WHY THIS MATTERS
origin/main is currently red on the blocking unit-test CI job. A red mainline blocks every downstream ticket in the Jul-2026 strategic sequence (#319→#323). This run IS the gating slice of P0 issue #319 ("restore green tsc/lint/vitest/build"). Nothing else ships until this is green.

ROOT CAUSE (already diagnosed — verify, don't re-derive from scratch)
The lane-rollover sensor scoring feature (#292) landed on top of the #282 physics-controller split. It added `getLaneSensors(): LaneSensorDef[]` to the real GameObjects (src/objects/object-core.ts) and a call to it inside `CollisionDispatcher.rebuildHandleCaches()` at src/game/physics/collision-dispatch.ts:159:
    for (const sensor of (this.host.gameObjects?.getLaneSensors() || [])) { ... }
The REAL runtime is fine — the method exists. But the feature did NOT update the shared test host-mocks. In these three files the mocked `gameObjects` is non-null but has no `getLaneSensors`, so `rebuildHandleCaches()` throws `getLaneSensors is not a function` and cascades:
    - tests/wasm-physics-parity.test.ts        (4 failures)
    - tests/collision-handle-space-scoring.test.ts (2 failures)
    - tests/free-map-test-mode.test.ts         (1 failure)
Separately, tests/lane-sensor-scoring.test.ts has TWO distinct problems:
    - a lint error: `'physics' is assigned a value but never used` at line ~251
    - a real behavioral failure: the test "clears per-ball debounce on ball:launched"
      does `rebuildHandleCaches()` → stepPhysics → expect awardScoreCalls===1, then
      emits `ball:launched`, stepPhysics again, expects awardScoreCalls===2 but gets 0.
      Determine whether this is (a) a genuine bug in the lane-sensor per-ball debounce
      reset on the `ball:launched` handler in collision-dispatch.ts, or (b) a test-setup
      artifact. If (a), fix the product code; if (b), fix the test — but do NOT weaken or
      delete the assertion to force a pass.

STRICT BOUNDARIES — files you MAY edit
    - tests/wasm-physics-parity.test.ts
    - tests/collision-handle-space-scoring.test.ts
    - tests/free-map-test-mode.test.ts
    - tests/lane-sensor-scoring.test.ts
    - src/game/physics/collision-dispatch.ts  ← ONLY if the debounce-reset failure is a
      genuine product bug (the `ball:launched` handler / lane-sensor per-ball debounce map).
    - a NEW shared test helper (e.g. tests/helpers/make-physics-host.ts) is encouraged so the
      `getLaneSensors: () => []` (and other gameObjects) stub isn't copy-pasted into three files.
      If you introduce it, refactor the three mock builders to use it, but keep each test's
      existing behavior and assertions identical.

DO NOT TOUCH: src/config.ts, src/game-elements/*-feeder.ts (that's a parallel Copilot lane, #306),
src/display/**, src/shaders/**, native/**, src/wasm/**, src/adventure/**, or any source file other
than the one collision-dispatch.ts exception above. Do NOT modify .github/workflows/** (CI hardening
is a separate Claude Code lane). Do NOT delete or skip tests to go green.

ITERATION DISCIPLINE (self-verify every loop)
1. At iteration 0: OVERWRITE .swarm-state.md fresh — it currently holds the completed #282
   split state; replace it with this run's objective, baseline, and plan.
2. Fix ONE cluster at a time (start with the shared-mock contract — it clears 7 of 8 fails).
   After each change run the targeted tests first, then the full suite:
       npx vitest run tests/wasm-physics-parity.test.ts tests/collision-handle-space-scoring.test.ts tests/free-map-test-mode.test.ts tests/lane-sensor-scoring.test.ts
       npx vitest run
       npm run lint
       npx tsc -b
3. Save progress to .swarm-state.md at every iteration boundary: what you changed, current
   pass/fail counts for all four commands, what's next, and any blocker. Noah must be able to
   pause and resume cleanly from that file alone.
4. When all four commands are green, run `npx vite build` once to confirm the bundle links, then
   write a final "Summary for Jules wrap-up" section to .swarm-state.md listing every file changed
   and a one-paragraph description of the fix. STOP — do not start new work.

ACCEPTANCE (all must hold on a fresh checkout)
    [ ] npx tsc -b       — clean
    [ ] npm run lint     — 0 errors (pre-existing warnings OK, but don't add new ones)
    [ ] npx vitest run   — 0 failed (target ~555 passed / 1 skipped)
    [ ] npx vite build   — succeeds
    [ ] no assertion weakened/deleted; the debounce test passes for the right reason
    [ ] .swarm-state.md reflects a completed run with a Jules wrap-up summary
```

## B. GitHub issue — decoupled Copilot lane (feeder `FEEDER_TUNABLES` config-only pass)

> Fully decoupled from kimi-cli's lane: kimi touches `tests/*` + `collision-dispatch.ts`; this touches `src/config.ts` + the 5 feeder files + `docs/feeder-audit.md` + NEW feeder test files. No file overlap. (Issue #306 already exists for this — expand it or file this as its refinement.)

```
Title: Externalize feeder tunables to FEEDER_TUNABLES (config-only pass) + per-feeder transition tests

Context / motivation
Active focus area: system-architecture cleanup → config extraction. The five feeders that give
the table its pachinko identity —
  src/game-elements/{mag-spin,nano-loom,prism-core,gauss-cannon,quantum-tunnel}-feeder.ts —
hardcode their catch radius, spin duration, release force, cooldown, and point values across five
separate files. Designers can't rebalance without hunting through source. PLAN.md §5 documents the
Mag-Spin state machine (IDLE→CATCH→SPIN→RELEASE→COOLDOWN); docs/feeder-audit.md already exists as
the landing spot for a drift matrix. This is explicitly a CONFIG-ONLY pass: ZERO physics, display,
or shader behavior changes.

Proposed approach (first pass — Noah will expand before handing to Copilot)
1. Add a `FEEDER_TUNABLES` block to src/config.ts, keyed per feeder, seeded with the CURRENT
   hardcoded values verbatim (no rebalancing in this PR).
2. Replace each hardcoded literal in the five feeder modules with a config read. No logic, timing,
   or force-curve changes — behavior must be byte-identical.
3. Add Vitest covering each feeder's state-machine transition table (pure-logic where possible;
   mock Babylon/Rapier per the repo convention — tests never import real engine code).
4. Update docs/feeder-audit.md with a PLAN.md §5-vs-implementation matrix and a Rationale column
   per tunable.

Acceptance criteria (rough — refine during expansion)
- [ ] All five feeders read every tunable from FEEDER_TUNABLES; no magic numbers remain in the five files.
- [ ] Default values reproduce current behavior exactly (existing tests unchanged & green).
- [ ] Per-feeder transition Vitest added; full suite green; tsc + lint clean.
- [ ] docs/feeder-audit.md answers: is PLAN.md §5 still ground truth?
- [ ] Zero edits outside config.ts, the 5 feeder files, docs/feeder-audit.md, and tests/.

Open questions for Noah to resolve during expansion
- Tunable table scope: only the 5 timing/force values, or also emissive/audio params per feeder?
- Is PLAN.md §5 still the intended ground truth, or has the Mag-Spin FSM drifted on purpose?
- Should the non-standard feeders (quantum-tunnel, gauss-cannon) share one schema or get their own?
```

## C. Three chat-model prompts targeting issue B

### C1 — Gemini Pro (codebase + completeness)
```
I'm working on a TypeScript/Vite/Babylon.js 7 + Rapier3D pachinko-pinball game (Pachinball). I want
you to help me produce a complete, dependency-aware implementation plan for the GitHub issue below.
I'll paste the repo alongside this; if any file isn't visible, tell me exactly which paths you'd
need to see rather than guessing.

Do these four things:
1. Identify every file and function that must change to satisfy the issue — especially the exact
   hardcoded literals in each of the five feeder modules
   (src/game-elements/{mag-spin,nano-loom,prism-core,gauss-cannon,quantum-tunnel}-feeder.ts) that
   need to move into a FEEDER_TUNABLES block in src/config.ts.
2. Spot missed dependencies: any place a feeder value is read indirectly, duplicated, or coupled to
   physics/display so that "config-only, zero behavior change" could accidentally be violated.
3. Propose the FEEDER_TUNABLES type shape (shared schema vs per-feeder), justified against how
   different the five feeders actually are.
4. Produce a step-by-step, PR-sized implementation plan with a self-verification checklist
   (tsc -b, npm run lint, npx vitest run) and a testing strategy that mocks Babylon/Rapier per the
   repo convention (tests never import real engine code).

--- ISSUE ---
[PASTE THE FULL TEXT OF ISSUE B HERE]
```

### C2 — Kimi.com / K2 (stress-test + alternatives)
```
Stress-test the proposed approach in the GitHub issue below for a TypeScript/Babylon.js pachinko
game. The stated constraint is "config-only pass, zero physics/display/shader behavior change,
default values reproduce current behavior exactly."

1. Attack the plan: where is "zero behavior change" most likely to silently break (float precision,
   init order, values read before config loads, shared mutable config objects, per-frame reads vs
   cached)? List concrete failure modes.
2. Generate 2 alternative designs to a single flat FEEDER_TUNABLES block — e.g. (a) per-feeder
   config classes with a shared interface + validation, (b) a data/JSON-driven feeder registry
   analogous to the game's existing JSON adventure-track schema. Sketch each.
3. Argue for the best of the three for THIS codebase (five feeders, some non-standard), given the
   goal is designer-rebalance-ability without a behavior regression, and recommend a concrete choice.

--- ISSUE ---
[PASTE THE FULL TEXT OF ISSUE B HERE]
```

### C3 — Grok.com (ecosystem currency)
```
Give me current (2026) ecosystem awareness relevant to this task before I implement it, so the
approach isn't already outdated. Stack: TypeScript ~5.9, Vite, Babylon.js core v7,
@dimforge/rapier3d-compat, ESLint 9 (flat config), Vitest, Playwright.

The task: extract hardcoded gameplay tunables from five modules into a central typed config block
for designer rebalancing (full issue below).

Tell me:
1. Current best practice (2026) for typed game-tuning config in a TS/Vite project — const-assertions
   vs schema-validated (zod/valibot) config, and whether pulling in a validator is worth it here or
   overkill for an internal tunables table.
2. Any Babylon.js 7 / Rapier-compat gotcha where reading a value from a shared config object every
   frame vs caching at init meaningfully affects perf or GC.
3. Whether anything in the current TS/Vite/ESLint-9 tooling changes how I'd structure or lint a
   large exported config constant (e.g. satisfies operator, no-magic-numbers rules).
Keep it specific and current; skip generic advice.

--- ISSUE ---
[PASTE THE FULL TEXT OF ISSUE B HERE]
```

## D. Copilot Agent handoff
```
Implement the following fully-specified GitHub issue in the Pachinball repo (TypeScript / Vite /
Babylon.js 7 / Rapier3D). Stay strictly within the stated file boundaries: edit ONLY src/config.ts,
the five feeder modules under src/game-elements/, docs/feeder-audit.md, and add test files under
tests/. This is a CONFIG-ONLY pass — do not change any physics, display, or shader behavior; default
tunable values must reproduce current behavior exactly. Do NOT touch tests/wasm-physics-parity.test.ts,
tests/collision-handle-space-scoring.test.ts, tests/free-map-test-mode.test.ts,
tests/lane-sensor-scoring.test.ts, or anything under src/game/physics/ (a separate agent owns those).

Verify your work before opening the PR: `npx tsc -b`, `npm run lint`, and `npx vitest run` must all
be green, and existing tests must pass unchanged. Open a PR; do not merge.

{{EXPANDED_ISSUE}}
```

## E. Claude Code whole-stack task — CI hardening + build/deploy pipeline verify
```
Pachinball whole-stack hygiene pass. This exercises the project's real pipeline (build → deploy →
live verify) AND advances the CI-hardening half of P0 issue #319. It is independent of the physics
test-fix work happening in parallel — do NOT touch tests/*, src/game/physics/*, src/config.ts, or
the feeder files. Scope is limited to CI config, docs, and a build/deploy dry-run.

Do these, in order, each verified before the next:

1. BUILD PIPELINE DRY-RUN (no deploy side effects)
   - `npx tsc -b`, `npm run lint`, `npx vitest run`, `npx vite build`. Report the status of each.
     (Expect these to be green only after the parallel red-mainline fix lands; if still red, STOP
     and report which — do not paper over it.)
   - Inspect deploy.py: confirm it reads SFTP creds from env (.env.deploy), NOT hardcoded (issue #288
     / PR #314). Do a DRY RUN only — parse/validate the config and print what it WOULD upload from
     dist/; do NOT actually SFTP to test.1ink.us. If deploy.py has no dry-run flag, add a --dry-run
     that lists the file manifest and target paths without connecting.

2. CI HARDENING (#319), edit ONLY .github/workflows/ci.yml + playwright.config.ts + docs:
   - Promote the desktop Playwright smoke job from `continue-on-error: true` to blocking.
   - Add a `mobile-chrome` smoke job (or matrix leg) running the mobile touch smoke spec, blocking.
   - Prefer adding a Playwright `webServer` block to playwright.config.ts (pointing at `npm run dev`
     on 5173) so e2e no longer depends on the hand-rolled inline `npm run dev &` poll in ci.yml;
     update ci.yml to match. Keep the WASM-excluded `vite build` step (no emcc on runner).
   - Run `npx playwright test tests/verify_prism_core.spec.ts` locally (or the mobile smoke) to
     confirm the webServer wiring actually boots the app before you rely on it in CI.

3. DOCS DRIFT (#319 acceptance):
   - Correct CLAUDE.md's stale "game.ts ~140 KB / monolithic ~140 KB" claim to the current reality
     (game.ts and the systems have been modularized — measure and state the real number).
   - Add/repair the "Adding an adventure track" section referencing the JSON track path
     (src/adventure/track-data/*.json + track-schema.ts) in docs/ARCHITECTURE.md.

Commit on the working branch, open a PR (do not merge), and in the PR body report: the four-command
build status, the deploy dry-run manifest summary, and exactly which CI jobs are now blocking vs
non-blocking.
```

## F. Jules wrap-up task — integrate kimi-cli's output (end-of-day template)

> Fill the three placeholders at end of day once kimi-cli finishes, then hand to Jules.

```
You are wrapping up and integrating the output of an autonomous kimi-cli run on the Pachinball repo
(TypeScript / Vite / Babylon.js 7 / Rapier3D) into a single reviewable PR. The run's objective was
to RESTORE A GREEN MAINLINE (fix 8 failing Vitest tests + 1 lint error caused by the #292 lane-sensor
scoring feature breaking the shared test host-mock contract).

What kimi-cli changed
- Files changed: {{KIMI_CLI_FILES_CHANGED}}
- Summary: {{KIMI_CLI_SUMMARY}}
- Known issues I noticed on cursory review: {{KNOWN_ISSUES}}

Wrap-up checklist (do all, in order):
1. Run the formatter if the repo has one; otherwise skip (this repo relies on ESLint, no Prettier).
2. `npm run lint` — fix any remaining lint errors (do NOT introduce new eslint-disable directives to
   hide them; fix the underlying issue).
3. `npx tsc -b` — must be clean.
4. `npx vitest run` — must be 0 failed. If any test fails, fix it WITHOUT weakening or deleting
   assertions. The "clears per-ball debounce on ball:launched" test must pass for the right reason.
5. Complete any TODO/stub/`// FIXME` kimi-cli left behind in the changed files.
6. Add unit tests for any NEW public function kimi-cli introduced that lacks coverage (e.g. a shared
   test-host helper is fine to leave untested, but new product code in collision-dispatch.ts is not).
7. Update inline docs / README / .swarm-state.md if any public API changed (the lane-sensor debounce
   behavior, if touched, should be documented).
8. `npx vite build` — must succeed (bundle links; WASM excluded, Rapier fallback).
9. Sanity-check that the diff is SCOPED: only tests/* and at most collision-dispatch.ts changed.
   If kimi-cli strayed into config.ts, feeders, display, shaders, native, or workflows, revert those
   out — they belong to other lanes.

Acceptance criteria (checklist for the PR):
- [ ] `npx tsc -b` clean
- [ ] `npm run lint` 0 errors
- [ ] `npx vitest run` 0 failed (~555 passed / 1 skipped)
- [ ] `npx vite build` succeeds
- [ ] no assertion weakened/deleted; debounce test passes for the right reason
- [ ] diff scoped to tests/* (+ collision-dispatch.ts only if a real bug was fixed)
- [ ] PR description explains the root cause (test-mock contract break from #292) and the fix

Open a PR for Noah to review. DO NOT MERGE.
```

## G. Review loop — two Gemini Pro prompts

### G1 — Review the raw kimi-cli diff (before Jules wraps it)
```
Review this diff from an autonomous agent that fixed a red mainline in a TypeScript/Babylon.js/Rapier
pachinko game. The objective was to fix 8 failing Vitest tests + 1 lint error caused by a lane-sensor
scoring feature (#292) adding `getLaneSensors()` to GameObjects + a call in
CollisionDispatcher.rebuildHandleCaches(), without updating three shared test host-mocks; plus a real
per-ball debounce-reset bug in the lane-sensor `ball:launched` handler.

Check specifically for:
1. Correctness: did it fix the debounce-reset test by fixing PRODUCT code (collision-dispatch.ts) or
   by weakening/deleting the assertion or mangling the test setup? Flag any assertion that was
   softened, any test made trivially true, or any `.skip`/`.only` left in.
2. Architectural drift: is the shared test-host mock helper (if introduced) a clean single source of
   truth, or did it just copy-paste `getLaneSensors: () => []` into three files? Did it leak into
   product code it shouldn't have (config, feeders, display, native, workflows)?
3. Performance: if collision-dispatch.ts changed, does the per-ball debounce map get reset/cleared
   correctly on ball:launched without leaking entries across balls, and without per-frame allocation
   in the hot rebuildHandleCaches / stepPhysics path?
Give me a bullet list of concrete issues ranked by severity, and a go/no-go on wrapping this into a PR.

--- DIFF ---
[PASTE THE kimi-cli DIFF HERE]
```

### G2 — Review the Jules PR against objective + checklist
```
Review this pull request for a TypeScript/Babylon.js/Rapier pachinko game. It was produced by a
wrap-up agent (Jules) integrating an autonomous kimi-cli run whose objective was: RESTORE GREEN
MAINLINE — fix 8 failing Vitest tests + 1 lint error from the #292 lane-sensor scoring feature
breaking the shared test host-mock contract, plus a real per-ball debounce-reset bug.

Judge the PR against BOTH:
A. The original objective — is the mainline actually green now? Verify the PR would make all four of
   `npx tsc -b`, `npm run lint`, `npx vitest run`, `npx vite build` pass, with 0 failed tests and no
   assertion weakened. Confirm the debounce test passes for the RIGHT reason (product fix, not test
   edit).
B. The wrap-up checklist — lint clean, tsc clean, no leftover TODO/stub, new public functions in
   product code covered by tests, docs/.swarm-state.md updated if API changed, and the diff strictly
   scoped to tests/* (+ collision-dispatch.ts only). Flag any scope creep into config/feeders/
   display/native/workflows.
Give a bullet list of blocking issues, a bullet list of nits, and a clear merge / request-changes
recommendation with reasons.

--- PR ---
[PASTE THE JULES PR DIFF OR LINK/DESCRIPTION HERE]
```

---

## Suggested timeline
- **Kickoff (T+0 to T+15m):** Paste §A into kimi-cli and start the loop. File/expand issue §B for Copilot.
- **Expansion (T+15m to T+75m, while kimi runs):** Run §C1/§C2/§C3 (Gemini/K2/Grok) against issue B; fold their output into `{{EXPANDED_ISSUE}}`; hand §D to Copilot Agent.
- **Mid-day:** Run §E (Claude Code CI-hardening + deploy dry-run) once the red-mainline fix has landed so its build steps are green.
- **End-of-day (after kimi finishes):** Fill §F placeholders from `.swarm-state.md`'s Jules-wrap-up summary; run §F (Jules → PR, no merge).
- **Review:** §G1 on the raw kimi diff before Jules; §G2 on the Jules PR.

## Open questions
1. **`recent_chats`/`conversation_search` unavailable** in this remote environment — no cross-project chat history informed this run. If bugs were noticed mid-other-project this week, they aren't captured here.
2. **Debounce-reset failure not ground-truthed** — I did not confirm whether "clears per-ball debounce on `ball:launched`" is a real product bug or a test-setup artifact. kimi-cli §A must decide and act accordingly.
3. **#288 (deploy creds)** shows OPEN on GitHub but PR #314 ("Remove hardcoded deploy credentials") looks merged — the §E deploy dry-run should confirm deploy.py is env-based and #288 can be closed.
4. **Did the red commits ever go through CI on `main`?** Worth checking the Actions tab — if the blocking unit job was red on `main` and the push landed anyway, that's a branch-protection gap #319 should close (require status checks to pass before merge).
